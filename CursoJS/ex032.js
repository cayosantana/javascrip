/*function soma (a, b) {
    return a + b
}
const somaValor = soma(2, 6)
const somaFinal = somaValor
console.log(somaFinal)*/

const somaArrow = (a, b) => {
    return a + b
}
const somaArrowValue = somaArrow(2, 6)
const somaArrowFinal = somaArrowValue
console.log(somaArrowFinal)